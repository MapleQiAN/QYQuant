[tool.uv.workspace]
members = ["packages/qysp", "backend"]

[project]
name = "qyquant-workspace"
version = "0.0.0"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "pandas>=2.0",
    "pytest>=9.0",
    "pytest-cov>=6.0",
    "pytest-flask>=1.3.0",
    "ruff>=0.4",
]
