# Story 2.3: 个人主页信息管理

Status: ready-for-dev

## Story

作为已登录用户，
我希望设置和修改我的头像、昵称和个人简介，
以便在策略广场和社区中展示我的个人身份。

## Acceptance Criteria (验收标准)

1. **更新个人信息**：已登录用户提交 `PATCH /api/v1/users/me`（昵称、简介、头像 URL），users 表更新成功，API 返回最新用户信息（含 updated_at 北京时间 ISO 8601+08:00）
2. **字段验证**：昵称长度限制 2–30 字符，超出返回 422 并说明限制；简介长度限制 ≤200 字符
3. **公开主页**：其他用户访问 `GET /api/v1/users/:id`，返回公开信息（昵称、头像、简介、注册时间），不返回手机号等敏感字段
4. **获取当前用户信息**：`GET /api/v1/users/me` 返回完整用户信息（含手机号脱敏、plan_level）

## Tasks / Subtasks

- [ ] Task 1: 重构 Users Blueprint — 个人信息 API (AC: #1, #2, #4)
  - [ ] 1.1 实现 `GET /api/v1/users/me`：需要认证，返回当前用户完整信息（手机号脱敏为 `138****8000`）
  - [ ] 1.2 实现 `PATCH /api/v1/users/me`：需要认证，支持更新 nickname/bio/avatar_url，字段验证（nickname 2-30 字符，bio ≤200 字符）
  - [ ] 1.3 返回的 `updated_at` 和 `created_at` 转为北京时间 ISO 8601 格式：`2026-03-14T18:00:00+08:00`
- [ ] Task 2: 公开用户主页 API (AC: #3)
  - [ ] 2.1 实现 `GET /api/v1/users/:id`：无需认证，返回公开字段（id, nickname, avatar_url, bio, created_at）
  - [ ] 2.2 软删除用户（deleted_at 非空）返回 404
  - [ ] 2.3 被封禁用户（is_banned=true）的主页仍可访问但显示标记
- [ ] Task 3: 序列化层 — 请求/响应 Schema (AC: #1-#4)
  - [ ] 3.1 创建 `UserPublicSchema`：公开字段序列化
  - [ ] 3.2 创建 `UserPrivateSchema`：完整字段序列化（含手机号脱敏）
  - [ ] 3.3 创建 `UserUpdateSchema`：PATCH 请求验证（marshmallow 或 flask-smorest schema）
- [ ] Task 4: 单元测试与集成测试 (AC: #1-#4)
  - [ ] 4.1 GET /users/me 测试：认证用户获取信息、未认证返回 401
  - [ ] 4.2 PATCH /users/me 测试：成功更新、昵称过长返回 422、简介过长返回 422
  - [ ] 4.3 GET /users/:id 测试：公开信息不含手机号、已删除用户返回 404
  - [ ] 4.4 时间格式测试：返回的时间为北京时间 ISO 8601+08:00

## Dev Notes

### 依赖前置 Story

- Story 2.1：User 模型已重构（phone/nickname/bio/avatar_url/role/plan_level 等字段）
- Story 2.2：JWT 认证+刷新机制已完成（`@jwt_required()` 装饰器可用）

### 现有 Users Blueprint 分析

当前 `backend/app/blueprints/users.py` 需要检查并可能重构：
- URL 前缀需确认是否已迁移到 `/api/v1/users`
- 需要添加 `/me` 快捷路由（当前用户）

### API 端点规范

**GET /api/v1/users/me** （需认证）
```json
Response: {
  "data": {
    "id": "uuid",
    "phone": "138****8000",
    "nickname": "量化小白",
    "avatar_url": "https://...",
    "bio": "热爱量化交易",
    "role": "user",
    "plan_level": "free",
    "onboarding_completed": false,
    "created_at": "2026-03-14T18:00:00+08:00",
    "updated_at": "2026-03-14T18:00:00+08:00"
  }
}
```

**PATCH /api/v1/users/me** （需认证）
```json
Request: {
  "nickname": "新昵称",
  "bio": "新简介",
  "avatar_url": "https://new-avatar.jpg"
}
Response: { "data": { /* 同 GET /users/me 完整用户信息 */ } }
Error 422: { "error": { "code": "VALIDATION_ERROR", "message": "昵称长度需在 2-30 字符之间" } }
```

**GET /api/v1/users/:id** （无需认证）
```json
Response: {
  "data": {
    "id": "uuid",
    "nickname": "量化小白",
    "avatar_url": "https://...",
    "bio": "热爱量化交易",
    "created_at": "2026-03-14T18:00:00+08:00"
  }
}
Error 404: { "error": { "code": "USER_NOT_FOUND", "message": "用户不存在" } }
```

### 手机号脱敏函数

```python
def mask_phone(phone: str) -> str:
    """138****8000"""
    if not phone or len(phone) < 7:
        return "***"
    return f"{phone[:3]}****{phone[-4:]}"
```

放在 `backend/app/utils/` 中，供多处复用。

### 时间格式化

使用 `qysp.utils.time.to_beijing()` 将 UTC 时间转为北京时间，序列化时输出 ISO 8601 格式：
```python
from qysp.utils.time import to_beijing

def format_datetime(dt):
    """输出: 2026-03-14T18:00:00+08:00"""
    return to_beijing(dt).isoformat()
```

### Project Structure Notes

**修改的文件：**
```
backend/app/
├── blueprints/
│   └── users.py            # 重构：添加 /me 和 /:id 端点
├── schemas.py              # 新增 UserPublicSchema/UserPrivateSchema/UserUpdateSchema
└── utils/
    └── phone.py            # 新增：手机号脱敏函数（或放入已有 utils 模块）
```

### References

- [Source: _bmad-output/planning-artifacts/epics.md — Epic 2 Story 2.3 验收标准]
- [Source: _bmad-output/planning-artifacts/architecture.md — API 响应格式、时区规范]
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md — 个人设置页设计]
- [Source: _bmad-output/planning-artifacts/prd.md — FR2 个人主页信息管理]
- [Source: backend/app/blueprints/users.py — 现有 users 蓝图（需重构）]
- [Source: packages/qysp/src/qysp/utils/time.py — to_beijing() 时区工具]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
