# Story 5.5: 策略搜索与条件筛选

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

作为用户，
我希望通过关键词搜索或按条件筛选策略广场中的策略，
以便在众多策略中快速找到符合我需求的策略。

## Acceptance Criteria

1. **Given** 用户在搜索框输入中文关键词（如"均线"）
   **When** 调用 `GET /api/v1/marketplace/strategies?q=均线`
   **Then** 使用 PostgreSQL tsvector + zhparser 全文搜索，返回名称/描述匹配的策略（FR61）

2. **Given** 用户展开筛选面板并选择条件（分类=趋势跟踪、最大回撤 ≤20%）
   **When** 提交筛选请求
   **Then** 返回满足所有条件的策略列表（FR62），支持与关键词搜索组合
   **And** 搜索和筛选结果均支持分页

## Tasks / Subtasks

- [ ] Task 1: 数据库 — 全文搜索基础设施 (AC: #1)
  - [ ] 1.1 创建 Alembic migration：安装 `zhparser` 扩展（`CREATE EXTENSION IF NOT EXISTS zhparser`）
  - [ ] 1.2 创建全文搜索配置：`CREATE TEXT SEARCH CONFIGURATION chinese (PARSER = zhparser)` + 添加映射
  - [ ] 1.3 添加 `title_tsv TSVECTOR` 和 `description_tsv TSVECTOR` 生成列或触发器
  - [ ] 1.4 创建 GIN 索引：`CREATE INDEX idx_strategies_title_tsv ON strategies USING GIN(title_tsv)`
  - [ ] 1.5 创建 GIN 索引：`CREATE INDEX idx_strategies_desc_tsv ON strategies USING GIN(description_tsv)`
  - [ ] 1.6 创建触发器：INSERT/UPDATE 时自动更新 tsvector 列
  - [ ] 1.7 验证中文分词效果（测试"均线趋势策略"分词结果）
- [ ] Task 2: 后端 API — 扩展列表接口支持搜索 (AC: #1, #2)
  - [ ] 2.1 修改 `GET /api/v1/marketplace/strategies` 支持查询参数：
    - `q`：关键词搜索（中文全文搜索）
    - `category`：分类筛选（精确匹配）
    - `tags`：标签筛选（数组交集匹配 `&&`）
    - `verified`：仅显示验证策略（`is_verified=true`）
    - `max_drawdown_lte`：最大回撤 ≤ 指定值（从 `display_metrics->>'max_drawdown'` 筛选）
    - `annual_return_gte`：年化收益 ≥ 指定值（从 `display_metrics->>'annual_return'` 筛选）
  - [ ] 2.2 搜索实现：
    ```python
    query = query.filter(
        db.or_(
            Strategy.title_tsv.match(search_term, postgresql_regconfig='chinese'),
            Strategy.description_tsv.match(search_term, postgresql_regconfig='chinese')
        )
    )
    ```
  - [ ] 2.3 JSONB 筛选实现：
    ```python
    query = query.filter(
        Strategy.display_metrics['max_drawdown'].astext.cast(db.Float) >= max_drawdown_lte
    )
    ```
  - [ ] 2.4 搜索结果按相关性排序（`ts_rank`），筛选结果按创建时间排序
  - [ ] 2.5 所有条件可组合使用
  - [ ] 2.6 编写测试：中文搜索、分类筛选、指标筛选、组合筛选、分页
- [ ] Task 3: 前端 — 搜索框组件 (AC: #1)
  - [ ] 3.1 在 `Marketplace.vue` 头部添加搜索框
  - [ ] 3.2 样式：胶囊形、白色背景、`#E5E5E5` 边框、搜索图标、placeholder "搜索策略名称、标的、标签…"
  - [ ] 3.3 防抖搜索：输入后 300ms 触发搜索请求
  - [ ] 3.4 搜索为空时显示全部策略
- [ ] Task 4: 前端 — 筛选 Chip 组件 (AC: #2)
  - [ ] 4.1 在搜索框下方添加横向排列的筛选 Chip
  - [ ] 4.2 Chip 列表：全部、趋势跟踪、均值回归、动量策略、多因子、年化 >20%、回撤 <10%、✓ 平台验证
  - [ ] 4.3 Chip 样式：
    - 默认：白色背景、`#E5E5E5` 边框、`#64748B` 文字、12px 字号、胶囊形 `border-radius: 9999px`
    - 激活：黄色 `#F5E642` 背景、`#D4C400` 边框、`#1A1A1A` 文字
  - [ ] 4.4 支持多选（分类+指标可组合）
  - [ ] 4.5 "全部" chip 为互斥选项（选中时清除其他筛选）
- [ ] Task 5: 前端 Store 扩展 (AC: #1, #2)
  - [ ] 5.1 在 `useMarketplaceStore.ts` 中添加 `filters` state：`{ q, category, tags, verified, maxDrawdownLte, annualReturnGte }`
  - [ ] 5.2 修改 `fetchStrategies()` 将 filters 作为查询参数传递
  - [ ] 5.3 添加 `setFilter(key, value)` 和 `clearFilters()` actions
  - [ ] 5.4 筛选变化时自动重新请求（watch + debounce）
- [ ] Task 6: 前端 — 空状态与结果提示 (AC: #1, #2)
  - [ ] 6.1 搜索/筛选无结果时显示空状态：说明文字 + "清除筛选" 按钮
  - [ ] 6.2 显示当前结果数量提示
- [ ] Task 7: 测试 (AC: #1, #2)
  - [ ] 7.1 后端测试：中文全文搜索、分类筛选、JSONB 指标筛选、组合条件、分页
  - [ ] 7.2 前端测试：搜索框防抖、Chip 选择/取消、筛选组合、空状态

## Dev Notes

### 架构约束与模式

- **前端框架**: Vue 3 + Composition API + TypeScript + Pinia
- **后端框架**: Flask + 蓝图，路由前缀 `/api/v1/`
- **数据库**: PostgreSQL + Alembic + zhparser 扩展
- **搜索方案**: PostgreSQL tsvector + zhparser（中文分词），MVP 阶段无需 Elasticsearch

### 全文搜索实现

```sql
-- 安装 zhparser 扩展
CREATE EXTENSION IF NOT EXISTS zhparser;

-- 创建中文搜索配置
CREATE TEXT SEARCH CONFIGURATION chinese (PARSER = zhparser);
ALTER TEXT SEARCH CONFIGURATION chinese ADD MAPPING FOR n,v,a,i,e,l WITH simple;

-- 添加 tsvector 列和索引
ALTER TABLE strategies ADD COLUMN title_tsv TSVECTOR
  GENERATED ALWAYS AS (to_tsvector('chinese', COALESCE(title, ''))) STORED;
ALTER TABLE strategies ADD COLUMN description_tsv TSVECTOR
  GENERATED ALWAYS AS (to_tsvector('chinese', COALESCE(description, ''))) STORED;

CREATE INDEX idx_strategies_title_tsv ON strategies USING GIN(title_tsv);
CREATE INDEX idx_strategies_desc_tsv ON strategies USING GIN(description_tsv);

-- 搜索查询示例
SELECT *, ts_rank(title_tsv, query) AS rank
FROM strategies, plainto_tsquery('chinese', '均线趋势') AS query
WHERE (title_tsv @@ query OR description_tsv @@ query)
  AND is_public = true AND review_status = 'approved'
ORDER BY rank DESC
LIMIT 20;
```

### JSONB 指标筛选

```sql
-- 最大回撤 ≤ 20%
WHERE (display_metrics->>'max_drawdown')::float >= -20.0

-- 年化收益 ≥ 20%
WHERE (display_metrics->>'annual_return')::float >= 20.0
```

### UX 设计要求

- **搜索框**: 胶囊形，顶部右侧，300px 宽度，placeholder 灰色 `#94A3B8`
- **筛选 Chip**: 横向排列，胶囊形 `border-radius: 9999px`，激活态黄色 `#F5E642`
- **空状态**: 说明文字 + "清除筛选" 按钮
- **防抖**: 搜索输入后 300ms 触发请求

### 前端代码结构

```
frontend/src/
├── views/
│   └── Marketplace.vue            # 修改：添加搜索框和筛选 Chip
└── stores/
    └── useMarketplaceStore.ts     # 修改：添加 filters state 和搜索逻辑
```

### 关键依赖

- **前置依赖**: Story 5.1（Marketplace 页面、marketplace 蓝图、列表接口）
- **外部依赖**: PostgreSQL zhparser 扩展需要在数据库服务器上安装

### 防踩坑提醒

- **不要** 使用 LIKE '%keyword%' 实现搜索 — 必须使用 tsvector + zhparser 全文搜索（FR61）
- **不要** 在前端实现筛选逻辑 — 所有筛选在后端 SQL 完成，前端只传参数
- **不要** 忘记搜索防抖 — 不加防抖会导致大量无效 API 请求
- **不要** 忘记处理 `display_metrics` 为 NULL 的情况 — JSONB 筛选需要 COALESCE 或 NULL 检查
- **zhparser 依赖**: 确认 PostgreSQL 环境已安装 zhparser 扩展，若未安装提供安装说明
- **JSONB 索引**: 若指标筛选性能不足，考虑添加 `CREATE INDEX ON strategies USING GIN(display_metrics)`

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 5.5]
- [Source: _bmad-output/planning-artifacts/prd.md#FR61, FR62]
- [Source: _bmad-output/planning-artifacts/architecture.md#Search Implementation]
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#Search & Filter UI]

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List
